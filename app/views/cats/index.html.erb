<h1>Cats!</h1>
<ul>
  <% @cats.each do |cat| %>
  <li>
    <h2> <%= cat.name %> </h2>
    <li> <%= cat.sex %> </li>
    <li> <%= cat.color %> </li>
    <li> <%= cat.birth_date %> </li>
    <li> <%= cat.description %> </li>
      <% end %>
    <br>
  </li>

</ul>
